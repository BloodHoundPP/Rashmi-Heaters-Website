
  # Website redesign proposal (Copy)

  This is a code bundle for Website redesign proposal (Copy). The original project is available at https://www.figma.com/design/EULPGIUg3f64EPdpMMXuda/Website-redesign-proposal--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  